{
  "name": "profile-gen",
  "version": "0.1.3",
  "description": "A simple profile site generator.",
  "homepage": "https://github.com/pineapplelol/profile-gen",
  "main": "src/cli.js",
  "bin": {
    "profile-gen": "src/cli.js"
  },
  "scripts": {
    "format": "prettier --config .prettierrc '{src,test}/*.ts' --write",
    "format:check": "prettier --config .prettierrc '{src,test}/*.ts' --check",
    "test": "jest"
  },
  "dependencies": {
    "command-line-args": "^5.1.1",
    "fs": "^0.0.1-security",
    "minify": "^7.0.2",
    "ora": "^5.4.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "@types/node": "^14.14.37",
    "jest": "^27.0.3",
    "prettier": "^2.3.0",
    "ts-jest": "^27.0.2",
    "typescript": "^4.3.2"
  }
}
